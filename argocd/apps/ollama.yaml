apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ollama
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    argocd.argoproj.io/sync-wave: "20"
spec:
  project: llmops

  source:
    repoURL: https://helm.otwld.com/
    chart: ollama
    targetRevision: "*"
    helm:
      releaseName: ollama
      values: |
        replicaCount: 1

        ollama:
          gpu:
            enabled: false
          models:
            - llama3.2
            - qwen2.5:7b

        persistentVolume:
          enabled: true
          size: 50Gi
          storageClass: gp3

        resources:
          requests:
            cpu: "500m"
            memory: "2Gi"
          limits:
            cpu: "2000m"
            memory: "8Gi"

        service:
          type: ClusterIP
          port: 11434

        ingress:
          enabled: false

        podAnnotations:
          app.kubernetes.io/part-of: llmops-platform

  destination:
    server: https://kubernetes.default.svc
    namespace: ollama

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false

    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true

    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  ignoreDifferences:
    - group: ""
      kind: Service
      jsonPointers:
        - /spec/clusterIP
        - /spec/clusterIPs

  info:
    - name: Description
      value: Ollama local LLM server for private-chat alias (CPU-only)
    - name: Models
      value: llama3.2, qwen2.5:7b
